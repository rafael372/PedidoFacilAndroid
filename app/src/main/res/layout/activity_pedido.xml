<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/colorBackground"
    android:padding="16dp"
    android:orientation="vertical" >
    <ListView
        android:id="@+id/lstItens"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/BttIncluir"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="16dp"
        android:stackFromBottom="true"
        android:transcriptMode="alwaysScroll" />

    <Button
        android:id="@+id/BttIncluir"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:background="@color/colorButton"
        android:textColor="@color/colorPrimary"
        android:onClick="onClickBttIncluir"
        android:text="Incluir"
        android:textStyle="bold"
        android:layout_above="@+id/BttSalvar"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/BttSalvar"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:background="@color/colorButton"
        android:textColor="@color/colorPrimary"
        android:onClick="onClickbttSalvar"
        android:text="Salvar"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
