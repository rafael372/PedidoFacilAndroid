<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.fatectq.pedidofacil">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_pedido_facil"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MesasActivity"
            android:label="@string/title_activity_mesas"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CadastrosActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ConsultaLoginActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".PedidoActivity" />
        <activity android:name=".ConsultaCategoriaActivity" />
        <activity android:name=".ConsultaProdutoActivity" />
        <activity android:name=".QtdProdActivity"></activity>
    </application>

</manifest>